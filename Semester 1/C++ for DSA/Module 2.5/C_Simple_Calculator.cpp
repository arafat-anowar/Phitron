#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x, y;
    cin >> x >> y;
    cout << x << " + " << y << " = " << (long long int)x + y << endl
         << x << " * " << y << " = " << (long long int)x * y << endl
         << x << " - " << y << " = " << x - y;
    return 0;
}